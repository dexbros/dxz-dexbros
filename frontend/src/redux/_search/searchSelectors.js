/** @format */

export const selectSearchResult = (state) => state.search.all;
export const selectSearchUser = (state) => state.search.people;
export const selectSearchBlock = (state) => state.search.searchblock;
export const selectSearchposts = (state) => state.search.searchposts;
